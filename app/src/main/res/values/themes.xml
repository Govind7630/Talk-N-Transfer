<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme -->
    <style name="Base.Theme.LoginModule" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Override colorOutline to prevent grey border -->
        <item name="colorOutline">@color/placeholder</item>
        <item name="colorControlNormal">@color/placeholder</item>
    </style>

    <style name="Theme.LoginModule" parent="Base.Theme.LoginModule" />
</resources>
